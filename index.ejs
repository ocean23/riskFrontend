<!DOCTYPE html>
<html lang="ch">
<head>
<meta charset="utf-8" />
<meta http-equiv="content-type" content="text/html; charset=utf-8" />
<meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no" />
<link rel="shortcut icon" href="/assets/iceworld/img/navLogo.png" type="image/x-icon">
<!-- yield "head" --><!-- /yield -->
<link rel="stylesheet" href="/public/css/main.css" />
<script src="/assets/common/js/util.js"></script>
<title>寻找冰封的宝藏</title>
</head>
	<body>
	<script type="text/javascript">
		const fullHost = location.protocol+'//'+location.hostname+(location.port ? ':'+location.port: '');
		const context = 'web';
		var G = G || {
			baseHost: '<%= htmlWebpackPlugin.options.baseHost%>',
			realHost: fullHost,
			serverHost: fullHost+'/'+context
		};
		G.id = function(id) {
			return document.getElementById(id);
		};
		var url_obj = parser(window.location.href);
		var code = url_obj.search.code;
		if (code === null || code === undefined) {
			window.location.href = 'redirecthome.html';
		} else {
			G.code = code;
		}
	</script>
		<div id="root">
		</div>
	</body>
</html>
